# Paths to a single audio/annotation pair for evaluation
audio_dir: /scratch/users/ntu/d230009/testset/nsf/audio/
textgrid_dir: /scratch/users/ntu/d230009/testset/nsf/textgrid/
rttm_dir: /scratch/users/ntu/d230009/testset/nsf/rttm/

# Chunking
chunk_length_s: 5.0

# DiCoW model (downloaded automatically from HuggingFace if not cached)
dicow_model: ./model/DiCoW

# Device: "auto" selects cuda if available, else cpu
device: auto

# Where to write hypotheses and scoring artifacts
output_dir: ./output

# Shared scorer and dataset identity. This evaluator imports the current
# checkout, not the stale scoring_dicow/ directory inside this experiment.
scoring_dicow_root: /home/hesiyuan/Desktop/projects/DiCoW_Experiments/scoring_dicow
testset_root: /scratch/users/ntu/d230009/testset
dataset_name: nsf
mapping: nsf
collar: 5
